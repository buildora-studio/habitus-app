name: CI

on:
  pull_request:
    branches: [main]

jobs:
  validate:
    name: Run Validations
    runs-on: ubuntu-latest

    steps:
      - name: 🛎️ Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 🧩 Enable pnpm
        run: corepack enable pnpm

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: "pnpm"

      - name: 📥 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 👓 Run lint
        run: pnpm lint

      - name: 🧪 Run test
        run: pnpm test:cov

      - name: ✅ Done
        run: echo "CI validations complete for changed apps ✅"
